## 查找
### ls

* 参数 -t 按时间排序
* 参数 -r 反转
* 结合下 -tr 结合可以显示最新改动的文件啦

### find
~~~bash
find 范围(例如 /) -name(不加上会打印所有文件，哈人) 要查找的文件名
~~~

### grep
~~~bash
grep "example" #查看当前文件夹中文件中含有“example”
grep  -r "example" #查看当前文件夹中,以及递归查找子文件夹中的
命令 | grep 想要的部分
~~~